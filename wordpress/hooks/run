
#!{{pkgPathFor "core/bash"}}/bin/bash

exec 2>&1

# start nginx with new config file
echo "starting nginx wite {{pkg.svc_config_path}}/nginx.conf"

exec nginx -c {{pkg.svc_config_path}}/nginx-site.conf
