suite:
  name: starkandwayne-consul
  system: docker-compose.yml
  task_service: tests

tests:
- name: standalone-exists
  spec:
  - wait: standalone-ready
  - wait: no-leader

tasks:
- name: standalone-ready
  command: /scripts/consul-ready
- name: no-leader
  command: /scripts/no-leader
